<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="applicationName" source="spring.application.name" defaultValue="portfolio"/>
    <springProperty scope="context" name="serviceName" source="portfolio.observability.service-name" defaultValue="${applicationName}"/>
    <springProperty scope="context" name="environment" source="portfolio.observability.environment" defaultValue="local"/>

    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <logLevel>
                    <fieldName>level</fieldName>
                </logLevel>
                <mdc/>
                <pattern>
                    <pattern>
                        {
                          "applicationName": "${applicationName}",
                          "serviceName": "${serviceName}",
                          "environment": "${environment}",
                          "hostName": "%property{HOSTNAME:-${HOSTNAME:-unknown}}",
                          "processId": "${PID:-}",
                          "thread": "%thread",
                          "logger": "%logger{40}",
                          "message": "%message"
                        }
                    </pattern>
                </pattern>
                <stackTrace>
                    <fieldName>stackTrace</fieldName>
                </stackTrace>
                <throwableClassName>
                    <fieldName>exceptionClass</fieldName>
                </throwableClassName>
                <throwableMessage>
                    <fieldName>exceptionMessage</fieldName>
                </throwableMessage>
            </providers>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON_CONSOLE"/>
    </root>
</configuration>
